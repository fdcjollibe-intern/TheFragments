<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="10dp"
    app:cardElevation="2dp">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="120dp">

        <!-- Photo thumbnail -->
        <ImageView
            android:id="@+id/ivPhoto"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:contentDescription="Photo" />

        <!--
            Top-right indicator area — stacks the dot and spinner on top of each other.
            Only one is visible at a time depending on upload state.
        -->
        <FrameLayout
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_gravity="top|end"
            android:layout_margin="6dp">

            <!-- Green/red sync dot -->
            <View
                android:id="@+id/viewSyncDot"
                android:layout_width="12dp"
                android:layout_height="12dp"
                android:layout_gravity="center"
                android:background="@drawable/bg_dot_unsynced" />

            <!-- Spinning progress — shown only while uploading, replaces dot visually -->
            <ProgressBar
                android:id="@+id/dotProgress"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_gravity="center"
                android:visibility="gone"
                style="@style/Widget.AppCompat.ProgressBar" />

        </FrameLayout>

    </FrameLayout>

</androidx.cardview.widget.CardView>